apiVersion: v1
kind: Service
metadata:
  name: temperature-service-node-port
spec:
  type: NodePort
  ports:
    - port: 30001
      targetPort: 8090
      nodePort: 30000
  selector:
    component: temperature-service